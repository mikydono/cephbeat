- key: cluster
  title: "cluster"
  description: >
    Ceph cluster metrics collected from Ceph.
  fields:
    - name: cluster
      type: group
      description: >
	`cluster` contains the metrics that were obtained from Ceph
      fields:
